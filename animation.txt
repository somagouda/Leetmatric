*{
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'times new roman', Times, serif;
}

body{
    background-color: rgb(89,90,106);
    color:white;
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    perspective: 1000px;
}

@property --progress-degree {
    syntax: '<percentage>';
    inherits: false;
    initial-value: 0%;
}

/* ---------- MAIN CARD ---------- */
.container{
    display: flex;               /* fixed: was "width: flex" */
    flex-direction: column;
    gap: 1rem;
    background-color: rgb(69,64,64);
    padding: 20px;
    border-radius: 5px;
    max-width: 600px;
    width: 50%;
    box-shadow: 0 0 10px rgba(0,0,0,0.1);

    transform-style: preserve-3d;
    transition: transform 0.4s ease, box-shadow 0.4s ease;
    animation: fadeSlideIn 0.7s ease-out;
}

.container:hover{
    transform: rotateX(3deg) rotateY(-3deg) translateY(-4px);
    box-shadow: 0 20px 40px rgba(0,0,0,0.4);
}

@keyframes fadeSlideIn{
    from{ opacity: 0; transform: translateY(40px) rotateX(-15deg); }
    to{ opacity: 1; transform: translateY(0) rotateX(0deg); }
}

h1{
    font-size: 2rem;
    animation: fadeSlideIn 0.8s ease-out;
}

/* ---------- USERNAME INPUT AREA ---------- */
.user-container{
    display: flex;
    flex-direction: column;
    gap: 10px;
    justify-content: start;
}

.user-input-container{
    display: flex;
    justify-content: space-between;
    gap: 10px;
}

#user-input{
    width: 80%;
    padding: 0.4rem;
    border-radius: 5px;
    border: 2px solid transparent;
    transition: box-shadow 0.3s ease, border-color 0.3s ease, transform 0.2s ease;
}

#user-input:focus{
    outline: none;
    border-color: #299f5d;
    box-shadow: 0 0 8px rgba(41,159,93,0.7);
    transform: scale(1.02);
}

#search-btn{
    padding: 0.4rem;
    width: 80%;
    border-radius: 5px;
    border: none;
    cursor: pointer;
    background: #299f5d;
    color: white;
    font-family: inherit;
    transition: transform 0.15s ease, box-shadow 0.15s ease, background 0.3s ease;
    box-shadow: 0 4px 0 #1d7444;
}

#search-btn:hover{
    background: #33b56b;
    transform: translateY(-2px);
    box-shadow: 0 6px 0 #1d7444;
}

#search-btn:active{
    transform: translateY(2px);
    box-shadow: 0 2px 0 #1d7444;
}

/* ---------- STATS + PROGRESS SECTION ---------- */
.stats-container{
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
}

/* this is the row that holds all 3 circles side by side */
.progress{
    display: flex;
    flex-direction: row;
    justify-content: center;
    align-items: center;
    gap: 1.5rem;
    flex-wrap: wrap;
}

/* ---------- CIRCLES ---------- */
.circle {
    width: 120px;
    height: 120px;
    border-radius: 50%;
    border: 4px solid #299f5d;
    position: relative;

    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;

    font-size: 16px;
    color: white;
    font-weight: 700;

    background: conic-gradient(
        #299f5d var(--progress-degree, 0%),
        #283a2e 0%
    );

    transform-style: preserve-3d;
    transition: transform 0.3s ease, box-shadow 0.3s ease, --progress-degree 1s ease;
    animation: circlePop 0.6s cubic-bezier(0.34, 1.56, 0.64, 1) backwards,
               floatCircle 3s ease-in-out infinite 1.2s;
}

.circle:hover{
    transform: rotateY(15deg) scale(1.08);
    box-shadow: 0 10px 25px rgba(41,159,93,0.5);
}

@keyframes circlePop{
    from{ opacity: 0; transform: scale(0.3) rotateY(90deg); }
    to{ opacity: 1; transform: scale(1) rotateY(0deg); }
}

@keyframes floatCircle{
    0%, 100%{ transform: translateY(0) rotateX(0deg); }
    50%{ transform: translateY(-6px) rotateX(4deg); }
}

/* stagger entrance: easy → medium → hard */
.easy-progress{ animation-delay: 0.2s, 1.2s; border-color: #299f5d; }
.medium-progress{ animation-delay: 0.4s, 1.4s; border-color: #ffb800; }
.hard-progress{ animation-delay: 0.6s, 1.6s; border-color: #ff4d4d; }

.medium-progress{
    background: conic-gradient(
        #ffb800 var(--progress-degree, 0%),
        #3a3324 0%
    );
}
.hard-progress{
    background: conic-gradient(
        #ff4d4d var(--progress-degree, 0%),
        #3a2424 0%
    );
}

.circle span,
.circle p {
    position: relative;
    z-index: 2;
}

/* ---------- STATS CARD (populated by JS) ---------- */
.stats-card{
    animation: fadeSlideIn 0.6s ease-out 0.8s backwards;
}